/**
 *array tutorials @arrayBasic.js
 *learning array data structure
 *Majata Hichimi 
 *19/01/2026
 */

 //arrays are a data structure used to store mutiple values
 //each value in an array has an index
 //the default index in an array is index 0

 //creating arrays in Javascript
 const userNames = ["Majata Hichimi","Mulima Hichimi","Bene HIchimi ","Lubono Hichimi"];

 //as stated each values are accessed via an index
 const firstUser = userNames[0];    //index 0 accesses the  first user in an array
 const secondUser = userNames[1];   //index 1 acesses the second user in the array
 const thirdUser = userNames [2];   //index 2 accesses the thrid user in the array
 const fourthUser = userNames[3];   //index 3 accesses the fourth user in an array
 
 //standard output
    console.log(`The users name is ${firstUser}`);
    console.log(`The users name is ${secondUser}`);
    console.log(`The users name is ${thirdUser}`);
    console.log(`The users name is ${fourthUser}`);

